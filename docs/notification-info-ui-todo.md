# Notification info UI follow-up

The Account screen already has an info action and BottomSheet for Discreet Notifications. Update that existing sheet (do not add another setting) to use the approved copy in `docs/discreet-notifications-info-copy.md`.

This is intentionally a copy-only UI change; notification privacy enforcement lives server-side in `notify-partner`.
