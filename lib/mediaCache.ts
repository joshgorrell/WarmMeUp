/**
 * Simple in-memory image cache for media URLs.
 * Clears all cached entries to force a re-fetch.
 */
export function clearLocalImageCache(): void {
  // In a full implementation this would clear an image cache library.
  // For now it's a no-op stub that satisfies the import contract.
}
